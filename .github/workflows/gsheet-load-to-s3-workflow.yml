name: Descargar archivo de Google Drive y almacenarlo en AWS S3 Bucket

on:
  push:
    branches:
      - main

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código fuente
      uses: actions/checkout@v2

    - name: Descarga de archivo Excel mediante wget
      run: |
        wget --no-check-certificate -0 prueba1.xlsx "https://docs.google.com/uc?export=download&id=13cCcKM6U_nXlFFxLmF0CUQkSSDOSZQFdJJLllK20Npw"

    - name: Mostrar contenido descargado
      run: |
        ls -al
